// Outputs the nth fibonacci numbers 
uint(8) f main(uint(8) n) { 
	
  uint(8) a = 0; 	
  uint(8) b = 1; 
  uint(8) fib;   

  uint(8) cntr = 1; 
  while ( cntr < n ) { 
  	cntr = cntr + 1;
	b = a + b;  
	a = b;
  }   			 

  POS(cntr == n); 
  f = a;     

}