const uint(2) en = 2;
int(32) x adder(int(32) a, int(32) b) { 
	
	x = a + b;  // perform check that c has the same type as return value
}


(int(32) c, ASYNC int(32) d, int(8) g) main(ASYNC int(32) e, int(32) f) {
	int(32) ar[3];// = 1;
	(ar[0]) = adder(e,f);
//	(ar[en]) = adder(ar[0],f);
	
	if ( en(0:0) || e(0:0) ) {	
	ar[2] = 2; }
	else {
	ar[2] = 1; }
//	ar[2] = 4;

	(c) = adder(1,f);
	POS(1); 
	(d(31:0)) = adder(f,e(31:0)); 
	
	g = e(0:7);
	POS(en == 1);
}


